import React from 'react'

const Home = (input,setInput) => {
  return (
    <div>
      This is home component lets see if this is working!!
      {input.count2}
        Another one

      {input.count2+1}
    </div>

  )
}

export default Home
